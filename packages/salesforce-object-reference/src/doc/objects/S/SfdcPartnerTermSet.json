{
  "SfdcPartnerTermSet": {
    "name": "SfdcPartnerTermSet",
    "description": "Represents a set of contractual terms for a partner relationship. SfdcPartnerTermSet is a Partner Business Org (PBO) standard object storing partner contract terms that are propagated from the Channel Orders Application (COA).",
    "properties": {
      "CoaSyncTimestamp": {
        "type": "string",
        "description": "Timestamp that indicates when the PBO refreshed data from the COA.",
        "format": "date-time"
      },
      "DefaultBillingFrequency": {
        "type": "number",
        "description": "Determines the default billing frequency in months for this channel contract."
      },
      "DefaultCancellationTerms": {
        "type": "number",
        "description": "Default number of days that the customer has to cancel an order."
      },
      "DefaultContractCurrency": {
        "type": "string",
        "description": "Determines the default currency for this channel contract.",
        "enum": []
      },
      "DefaultRenewalLength": {
        "type": "number",
        "description": "Default renewal length in months."
      },
      "IsDefaultContractAutoRenew": {
        "type": "boolean",
        "description": "Default value for whether a contracts auto-renews."
      },
      "LastReferencedDate": {
        "type": "string",
        "description": "The timestamp when the current user last accessed this record indirectly, for example, through a list view or related record.",
        "format": "date-time"
      },
      "LastViewedDate": {
        "type": "string",
        "description": "The timestamp when the current user last viewed this record or list view. If this value is null, the user might have only accessed this record or list view (LastReferencedDate), but not viewed it.",
        "format": "date-time"
      },
      "Name": {
        "type": "string",
        "description": "Required. The name of the partner term set."
      },
      "OverrideLevel": {
        "type": "string",
        "description": "Determines the level of override allowed for contract terms on orders.",
        "enum": []
      },
      "PartnerApiKey": {
        "type": "string",
        "description": "The API Key for the channel contract record on used to validate a request."
      },
      "PartnerContractTerm": {
        "type": "string",
        "description": "Partner term set copied from the COA."
      },
      "PartnerContractTermPbo": {
        "type": "string",
        "description": "18 character ID that references the PBO."
      },
      "Status": {
        "type": "string",
        "description": "Status of the Partner Contract Terms record in the COA.",
        "enum": []
      },
      "Type": {
        "type": "string",
        "description": "The type of Partner Contract Terms record in the COA, which determines where the partner sells.",
        "enum": []
      }
    },
    "module": "Core Salesforce",
    "sourceUrl": "https://developer.salesforce.com/docs/atlas.en-us.262.0.object_reference.meta/object_reference/sforce_api_objects_sfdcpartnertermset.htm",
    "x-version": "66.0",
    "clouds": [
      "Core Salesforce"
    ]
  }
}