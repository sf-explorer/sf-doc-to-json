{
  "VoiceCallInsight": {
    "name": "VoiceCallInsight",
    "description": "Represents the voice call insight data associated with a voice call. Each record represents the call insight of a specific recording or transcript within a call.",
    "properties": {
      "EngagementInsightType": {
        "type": "string",
        "description": "Required. The unique identifier of the platform setup entity that defines the configuration for this engagement insight type."
      },
      "GenerationDateTime": {
        "type": "string",
        "description": "Required. The timestamp when the call insight was generated.",
        "format": "date-time"
      },
      "InsightConfigName": {
        "type": "string",
        "description": "The name of the insight configuration, or category, used to classify the insight."
      },
      "InsightLanguage": {
        "type": "string",
        "description": "The language associated with the insight type. Each insight type is currently limited to a single language. If the same insight type is mapped to a different language, a new insight type is created.",
        "enum": []
      },
      "InsightModel": {
        "type": "string",
        "description": "Required. The category of the insight type.",
        "enum": []
      },
      "InsightOccurrenceCount": {
        "type": "number",
        "description": "The number of times a particular insight occurred in the transcript."
      },
      "InsightSubject": {
        "type": "string",
        "description": "The specific text, phrase, or subject identified in the voice call transcript that serves as the basis for the insight."
      },
      "InsightText": {
        "type": "string",
        "description": "The text content of the insight derived from the voice call transcript."
      },
      "Name": {
        "type": "string",
        "description": "The name of the insight type."
      },
      "Scope": {
        "type": "string",
        "description": "Required. The scope of the insight type.",
        "enum": []
      },
      "VoiceCallId": {
        "type": "string",
        "description": "Required. ID of the associated parent VoiceCall.",
        "format": "reference"
      },
      "VoiceCallRecordingId": {
        "type": "string",
        "description": "ID of the associated parent VoiceCallRecording.",
        "format": "reference"
      },
      "VoiceCallTranscript": {
        "type": "string",
        "description": "The transcript of the voice call. This field requires the canUserDoCloudIntegration permission."
      }
    },
    "module": "Core Salesforce",
    "sourceUrl": "https://developer.salesforce.com/docs/atlas.en-us.262.0.object_reference.meta/object_reference/sforce_api_objects_voicecallinsight.htm",
    "x-version": "66.0",
    "accessRules": "the Conversation Insights for Sales permission set",
    "clouds": [
      "Core Salesforce"
    ]
  }
}