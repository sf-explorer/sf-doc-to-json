{
  "BillingBatchScheduler": {
    "name": "BillingBatchScheduler",
    "description": "Represents a scheduled processing job that triggers recurring invoice batch runs.",
    "properties": {
      "BillingSchedulerName": {
        "type": "string",
        "description": "Required. The name of the scheduler."
      },
      "Comments": {
        "type": "string",
        "description": "Additional details about the billing batch scheduler."
      },
      "CronExpression": {
        "type": "string",
        "description": "Required. This field determines how often the scheduler recurs."
      },
      "EndDate": {
        "type": "string",
        "description": "The date when the scheduler stops triggering batch processing jobs.",
        "format": "date"
      },
      "FrequencyCadence": {
        "type": "string",
        "description": "Required. The frequency of the scheduler triggering the invoice batch run.",
        "enum": []
      },
      "FrequencyOptions": {
        "type": "string",
        "description": "This field is a derived field that stores the scheduler configuration."
      },
      "JobType": {
        "type": "string",
        "description": "Required. The type of batch processing job that the scheduler triggers.",
        "enum": []
      },
      "LastReferencedDate": {
        "type": "string",
        "description": "The timestamp when the current user last accessed a billing batch scheduler record indirectly, for example, through a list view or related record.",
        "format": "date-time"
      },
      "LastViewedDate": {
        "type": "string",
        "description": "The timestamp when the current user last viewed a billing batch scheduler record. If this value is null, it’s possible that the user only accessed the billing batch scheduler record or a related list view (LastReferencedDate), but not viewed the billing batch scheduler record itself.",
        "format": "date-time"
      },
      "NextRunTime": {
        "type": "string",
        "description": "The date and timestamp of the next scheduled batch invoice run in the user's time zone.",
        "format": "date-time"
      },
      "OwnerId": {
        "type": "string",
        "description": "Required. The ID of the user who created the scheduler.",
        "format": "reference"
      },
      "RecurringSubType": {
        "type": "string",
        "description": "The frequency at which the batch processing job recurs when the FrequencyCadence field value is set to Monthly.",
        "enum": []
      },
      "RecurringType": {
        "type": "string",
        "description": "The frequency at which the batch processing job is repeated when the FrequencyCadence field value is set to Weekly.",
        "enum": []
      },
      "RecursOn": {
        "type": "string",
        "description": "The interval at which the scheduler triggers a batch processing job.",
        "enum": []
      },
      "RecursOnDate": {
        "type": "string",
        "description": "The date on which the scheduler triggers a batch processing job."
      },
      "RecursOnDay": {
        "type": "string",
        "description": "The day on which the scheduler triggers a batch processing job.",
        "enum": []
      },
      "RunCriteriaId": {
        "type": "string",
        "description": "Required. The ID of the filter criteria that’s defined for the invoice batch run or the payment batch run.",
        "format": "reference"
      },
      "ShouldExcludeWkendAndHldy": {
        "type": "boolean",
        "description": "Required. Indicates whether weekends and holidays are excluded from the billing schedule (true) or not (false)."
      },
      "ShouldStartRunImmediately": {
        "type": "boolean",
        "description": "Required. Indicates whether the invoice scheduler must start the run immediately (true) or not (false)."
      },
      "StartDate": {
        "type": "string",
        "description": "Required. The date when the scheduler triggers its first batch processing job.",
        "format": "date"
      },
      "StartTime": {
        "type": "string",
        "description": "Required. The time when the scheduler triggers the batch processing job."
      },
      "Status": {
        "type": "string",
        "description": "Required. The status of the scheduler. Only active schedulers can trigger batch processing jobs.",
        "enum": []
      },
      "TimeZone": {
        "type": "string",
        "description": "The time zone is either the value selected when the run was configured, or it's the user's time zone. The time zone is shown in Greenwich Mean Time (GMT).",
        "enum": []
      }
    },
    "module": "Revenue Lifecycle Management",
    "sourceUrl": "https://developer.salesforce.com/docs/atlas.en-us.262.0.revenue_lifecycle_management_dev_guide.meta/revenue_lifecycle_management_dev_guide/sforce_api_objects_billingbatchscheduler.htm",
    "accessRules": "the Billing Admin permission set to access this object",
    "clouds": [
      "Revenue Lifecycle Management",
      "Core Salesforce"
    ],
    "x-version": "62.0"
  }
}