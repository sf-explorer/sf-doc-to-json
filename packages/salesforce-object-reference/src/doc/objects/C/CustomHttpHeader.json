{
  "CustomHttpHeader": {
    "name": "CustomHttpHeader",
    "description": "Represents a custom HTTP header used with OData 2.0 or OData 4.0 external data sources. Custom HTTP headers provide context information from Salesforce such as region, org details, or the role of the person viewing the external object.",
    "properties": {
      "Id": {
        "type": "string",
        "description": "Custom HTTP Header ID",
        "format": "salesforce-id",
        "maxLength": 18,
        "nullable": false,
        "readOnly": true
      },
      "CreatedDate": {
        "type": "string",
        "description": "Created Date",
        "format": "date-time",
        "nullable": false,
        "readOnly": true
      },
      "CreatedById": {
        "type": "string",
        "description": "Created By ID",
        "format": "salesforce-id",
        "x-object": "User",
        "maxLength": 18,
        "nullable": false,
        "readOnly": true
      },
      "LastModifiedDate": {
        "type": "string",
        "description": "Last Modified Date",
        "format": "date-time",
        "nullable": false,
        "readOnly": true
      },
      "LastModifiedById": {
        "type": "string",
        "description": "Last Modified By ID",
        "format": "salesforce-id",
        "x-object": "User",
        "maxLength": 18,
        "nullable": false,
        "readOnly": true
      },
      "SystemModstamp": {
        "type": "string",
        "description": "System Modstamp",
        "format": "date-time",
        "nullable": false,
        "readOnly": true
      },
      "ParentId": {
        "type": "string",
        "description": "ID of the entity to which this custom HTTP header is related.",
        "format": "salesforce-id",
        "x-objects": [
          "ExternalDataSource",
          "NamedCredential"
        ],
        "maxLength": 18,
        "nullable": false,
        "readOnly": true
      },
      "HeaderFieldName": {
        "type": "string",
        "description": "Name of the header field. The name must contain at least one alphanumeric character or underscore. It can also include: ! # $ % & ' * + - . ^ _ ` | ~",
        "maxLength": 255,
        "nullable": false,
        "readOnly": true
      },
      "HeaderFieldValue": {
        "type": "string",
        "description": "A formula that resolves to the value for the header. The values in the formula must evaluate to a string. If the formula resolves to null and an empty string, the header isn’t sent.",
        "maxLength": 1000,
        "nullable": false,
        "readOnly": true
      },
      "IsActive": {
        "type": "boolean",
        "description": "Indicates whether the custom HTTP header is available to use (true) or unavailable (false).",
        "nullable": false,
        "readOnly": true
      },
      "Description": {
        "type": "string",
        "description": "A text description of the header field’s purpose.",
        "maxLength": 1000,
        "nullable": true,
        "readOnly": true
      }
    },
    "module": "Tooling API",
    "keyPrefix": "0XH",
    "label": "Custom HTTP Header",
    "sourceUrl": "https://developer.salesforce.com/docs/atlas.en-us.api_tooling.meta/api_tooling/tooling_api_objects_customhttpheader.htm",
    "createable": false,
    "updateable": false,
    "deletable": false,
    "queryable": true,
    "searchable": false,
    "accessRules": "As of Spring ’20 and later, only authenticated internal and external users can access this object.",
    "clouds": [
      "Core Salesforce",
      "Core Salesforce",
      "Tooling API"
    ]
  }
}