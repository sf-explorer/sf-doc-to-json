{
  "AsyncOpSyndicationFeedFile": {
    "name": "AsyncOpSyndicationFeedFile",
    "description": "Represents the sync status of file-related information shared with external channels such as Facebook and Instagram.",
    "properties": {
      "AsyncOpSyndicationFeedFileNumber": {
        "type": "string",
        "description": "ID assigned to each syndication feed file record, and used for tracking and reference purposes."
      },
      "AsyncOperationTrackerId": {
        "type": "string",
        "description": "ID assigned to the Async Operation Tracker record, which monitors and manages the lifecycle of the syndication process.",
        "format": "reference"
      },
      "FeedContentBody": {
        "type": "string",
        "description": "The content of the feed file that is syndicated to the external platform."
      },
      "FeedContentContentType": {
        "type": "string",
        "description": "Specifies the format of the feed file to ensure proper processing. For example, CSV, JSON, or XML files.",
        "enum": []
      },
      "FeedContentLength": {
        "type": "number",
        "description": "The size of the feed file in bytes, which is used for validation and processing requirements."
      },
      "FeedContentName": {
        "type": "string",
        "description": "The name of the feed file, which includes identifiers like timestamp or sequence number."
      },
      "FeedScope": {
        "type": "string",
        "description": "Defines the scope or category of the feed. For example, if the feed applies to main, country-specific, or language-specific catalog segments.",
        "enum": []
      },
      "LastReferencedDate": {
        "type": "string",
        "description": "For internal use only.",
        "format": "date-time"
      },
      "LastViewedDate": {
        "type": "string",
        "description": "For internal use only.",
        "format": "date-time"
      },
      "PlatformConnections": {
        "type": "string",
        "description": "The external channel or destination for syndication.",
        "enum": []
      },
      "SyncMode": {
        "type": "string",
        "description": "Specifies the type of sync being performed.",
        "enum": []
      }
    },
    "module": "Core Salesforce",
    "sourceUrl": "https://developer.salesforce.com/docs/atlas.en-us.262.0.object_reference.meta/object_reference/sforce_api_objects_asyncopsyndicationfeedfile.htm",
    "clouds": [
      "Core Salesforce"
    ],
    "x-version": "64.0"
  }
}