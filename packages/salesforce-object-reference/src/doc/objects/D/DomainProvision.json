{
  "DomainProvision": {
    "name": "DomainProvision",
    "description": "Represents provisioned changes to custom domains.",
    "properties": {
      "CnameTarget": {
        "type": "string",
        "description": "Represents the canonical name (CNAME) of the external host or server for this domain. If you use a non-Salesforce provider, such as your own external server or CDN provider, to serve your domain, this field points to the CNAME of the external provider."
      },
      "IsActive": {
        "type": "boolean",
        "description": "Represents whether the provisioning for this domain is processing or complete (true) or not (false). When you activate a provisioned domain, this field is set to false."
      },
      "ProvisionEndDate": {
        "type": "string",
        "description": "Represents when the provisioning process was finished.",
        "format": "date-time"
      },
      "ProvisionFinalizeDate": {
        "type": "string",
        "description": "Represents when the provisioning process was canceled or when the provisioned domain was activated.",
        "format": "date-time"
      },
      "ProvisionStartDate": {
        "type": "string",
        "description": "Represents when the provisioning process started.",
        "format": "date-time"
      },
      "TargetCdnCertificate": {
        "type": "string",
        "description": "If the domain is served on the Salesforce Content Delivery Network (CDN), represents the certificate that serves this domain."
      },
      "TargetDomainName": {
        "type": "string",
        "description": "Represents the domain name, such as www.example.com."
      }
    },
    "module": "Tooling API",
    "sourceUrl": "https://developer.salesforce.com/docs/atlas.en-us.262.0.api_tooling.meta/api_tooling/tooling_api_objects_domainprovision.htm",
    "accessRules": "Salesforce Integration",
    "clouds": [
      "Tooling API"
    ],
    "x-version": "50.0"
  }
}